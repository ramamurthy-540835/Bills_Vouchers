services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      GOOGLE_APPLICATION_CREDENTIALS: /run/secrets/gcp_credentials.json
    secrets:
      - gcp_credentials
secrets:
  gcp_credentials:
    file: ./gcp-credentials.json
