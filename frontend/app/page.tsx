"use client"
import {ChangeEvent,useEffect,useMemo,useState} from "react"
import {useRouter} from "next/navigation"
type Extraction={vendor_name?:string;invoice_number?:string;gstin?:string;subtotal?:string;cgst?:string;sgst?:string;igst?:string;total_amount?:string}
type Doc={id:string;document_type:string;status:string;original_filename:string;uploaded_at:string;processing_error?:string|null;extraction?:Extraction|null}
type User={full_name:string;role:string}
const money=(v?:string|null)=>v?`₹${Number(v).toLocaleString("en-IN",{minimumFractionDigits:2})}`:"—"
export default function Home(){const router=useRouter(),[user,setUser]=useState<User|null>(null),[docs,setDocs]=useState<Doc[]>([]),[tab,setTab]=useState<"overview"|"documents"|"search">("overview"),[busy,setBusy]=useState(false),[message,setMessage]=useState(""),[q,setQ]=useState(""),[results,setResults]=useState<any[]>([]);async function csrf(){const r=await fetch("/api/auth/csrf");return r.ok?(await r.json()).token:""}async function load(){const me=await fetch("/api/auth/me");if(!me.ok){router.push("/login");return};setUser(await me.json());const r=await fetch("/api/documents");if(r.ok)setDocs(await r.json())}useEffect(()=>{load()},[]);const counts=useMemo(()=>({total:docs.length,review:docs.filter(d=>d.status==="needs_review").length,posted:docs.filter(d=>d.status==="posted").length,failed:docs.filter(d=>d.status==="failed"||d.status==="scan_failed").length}),[docs]);async function upload(e:ChangeEvent<HTMLInputElement>){const file=e.target.files?.[0];if(!file)return;setBusy(true);setMessage("Uploading to private GCS…");const fd=new FormData();fd.append("document_type","bill");fd.append("file",file);const r=await fetch("/api/documents/upload",{method:"POST",headers:{"x-csrf-token":await csrf()},body:fd});setBusy(false);if(!r.ok){setMessage("Upload failed. Please try again.");return};setMessage("Uploaded. Start scanning from the document list.");load()}async function scan(id:string){setBusy(true);setMessage("Scan queued — Gemini is extracting GST fields…");const r=await fetch(`/api/documents/${id}/scan`,{method:"POST",headers:{"x-csrf-token":await csrf()}});setBusy(false);if(!r.ok){setMessage("Scan could not be queued. Open the card to review available information.");return};setMessage("Scan queued. Refresh shortly for extracted GST fields.");load()}async function search(){if(!q.trim())return;setBusy(true);const r=await fetch(`/api/documents/search?q=${encodeURIComponent(q)}&top_k=8`);setBusy(false);if(r.ok)setResults((await r.json()).results);else setMessage("Search failed. Check GCP configuration.")}async function logout(){await fetch("/api/auth/logout",{method:"POST",headers:{"x-csrf-token":await csrf()}});router.push("/login")}if(!user)return <div className="loading">Loading finance workspace…</div>;return <div className="shell"><aside><div className="logo"><span>BV</span><div>Bills &amp; Voucher<small>Finance workspace</small></div></div><nav><button className={tab==="overview"?"active":""} onClick={()=>setTab("overview")}>Overview</button><button className={tab==="documents"?"active":""} onClick={()=>setTab("documents")}>GST documents</button><a className="nav-link" href="/gst/filing">GST Filing &amp; ITC</a><button className={tab==="search"?"active":""} onClick={()=>setTab("search")}>Semantic search</button></nav><div className="side-bottom"><div className="avatar">{user.full_name[0]}</div><div><strong>{user.full_name}</strong><small>{user.role}</small></div><button className="ghost" onClick={logout}>↪</button></div></aside><main className="workspace"><header><div><p className="eyebrow">FINANCE CONTROL PLANE</p><h1>{tab==="overview"?`Good morning, ${user.full_name.split(" ")[0]}`:tab==="documents"?"GST documents":"Find related evidence"}</h1></div><div className="header-actions"><label className="upload"><input type="file" accept="image/jpeg,image/png,application/pdf" onChange={upload} disabled={busy}/>+ Upload bill</label><button className="outline" onClick={()=>setTab("search")}>Search evidence</button></div></header>{message&&<div className="notice">{message}</div>}{tab==="overview"&&<><section className="hero"><div><p className="eyebrow">BIGQUERY + GEMINI + VECTOR SEARCH</p><h2>From source document to trusted finance data.</h2><p>Upload a bill or voucher to private GCS. Gemini extracts GST fields for review, then BigQuery stores the record and its semantic embedding.</p><button onClick={()=>setTab("documents")}>Open document queue →</button></div><div className="hero-orbit"><span>GCS</span><i>→</i><span>GEMINI</span><i>→</i><span>BIGQUERY</span><i>→</i><span>VECTOR</span></div></section><section className="metrics"><Metric label="Documents" value={counts.total} tone="blue"/><Metric label="Needs review" value={counts.review} tone="orange"/><Metric label="Posted" value={counts.posted} tone="green"/><Metric label="Failed" value={counts.failed} tone="red"/></section><section className="panel"><div className="panel-head"><h3>Latest documents</h3><button className="text-button" onClick={()=>setTab("documents")}>View all →</button></div><DocumentTable docs={docs.slice(0,6)} onScan={scan}/></section></>}{tab==="documents"&&<section className="panel full"><div className="panel-head"><div><p className="eyebrow">SOURCE EVIDENCE</p><h3>Bill and voucher queue</h3></div><label className="upload compact"><input type="file" accept="image/jpeg,image/png,application/pdf" onChange={upload} disabled={busy}/>+ Upload</label></div><DocumentTable docs={docs} onScan={scan}/></section>}{tab==="search"&&<section className="search-layout"><div className="search-card"><p className="eyebrow">BIGQUERY VECTOR_SEARCH</p><h2>Find similar bills and vouchers</h2><p className="muted">Search by meaning, vendor, GST purpose or invoice context.</p><div className="search-box"><input placeholder="e.g. office supplies from Bengaluru" value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==="Enter"&&search()}/><button onClick={search} disabled={busy}>Search</button></div></div><div className="results">{results.length===0?<div className="empty">Your semantic results will appear here.</div>:results.map((r,i)=><div className="result" key={r.document_id||i}><div><strong>{r.vendor_name||"Unknown vendor"}</strong><p>{r.invoice_number||"No invoice number"} · {r.gstin||"GSTIN unavailable"}</p></div><span>{money(r.total_amount)}</span></div>)}</div></section>}</main></div>}
function Metric({label,value,tone}:{label:string;value:number;tone:string}){return <div className={`metric ${tone}`}><span>{label}</span><strong>{value}</strong><small>BigQuery live count</small></div>}
function DocumentTable({docs,onScan}:{docs:Doc[];onScan:(id:string)=>void}){const[selected,setSelected]=useState<Doc|null>(null),close=()=>setSelected(null);return <><div className="table-wrap">{docs.length===0?<div className="empty">No documents yet. Upload a GST bill or voucher to begin.</div>:<table><thead><tr><th>Document</th><th>Type</th><th>Vendor</th><th>Total</th><th>Status</th><th></th></tr></thead><tbody>{docs.map(d=><tr key={d.id} onClick={()=>setSelected(d)} style={{cursor:"pointer"}}><td><strong>{d.original_filename}</strong><small>{new Date(d.uploaded_at).toLocaleString()}</small></td><td><span className="type">{d.document_type}</span></td><td>{d.extraction?.vendor_name||"Not scanned"}</td><td>{money(d.extraction?.total_amount)}</td><td><span className={`status ${d.status}`}>{d.status.replace("_"," ")}</span></td><td>{d.status==="scanning"?<button className="small-button" disabled>Scanning…</button>:d.status==="uploaded"||d.status==="failed"||d.status==="scan_failed"?<button className="small-button" onClick={e=>{e.stopPropagation();onScan(d.id)}}>Scan</button>:<button className="small-button" onClick={e=>{e.stopPropagation();setSelected(d)}}>Evidence</button>}</td></tr>)}</tbody></table>}</div>{selected&&<div style={{position:"fixed",inset:0,background:"#10263e88",zIndex:30}} onClick={close}><section onClick={e=>e.stopPropagation()} style={{position:"absolute",right:0,top:0,bottom:0,width:"min(480px,100%)",background:"#fff",color:"#14253b",padding:28,overflow:"auto",boxShadow:"-18px 0 50px #07182744"}}><button onClick={close} aria-label="Close document card" style={{float:"right",background:"transparent",fontSize:25}}>×</button><p className="eyebrow">DOCUMENT REVIEW · SAME-PAGE CARD</p><h2 style={{fontSize:22,marginRight:30}}>{selected.original_filename}</h2><p><b>Status:</b> {selected.status.replace("_"," ")}</p><hr style={{border:0,borderTop:"1px solid #e5ebf0",margin:"20px 0"}}/><h3 style={{fontSize:16}}>Extracted GST fields</h3><p><b>Vendor:</b> {selected.extraction?.vendor_name||"Not scanned"}</p><p><b>Invoice number:</b> {selected.extraction?.invoice_number||"Not available"}</p><p><b>GSTIN:</b> {selected.extraction?.gstin||"Not available"}</p><p><b>Subtotal:</b> {money(selected.extraction?.subtotal)}</p><p><b>CGST / SGST / IGST:</b> {money(selected.extraction?.cgst)} / {money(selected.extraction?.sgst)} / {money(selected.extraction?.igst)}</p><p><b>Total:</b> {money(selected.extraction?.total_amount)}</p><p><b>Uploaded:</b> {new Date(selected.uploaded_at).toLocaleString()}</p>{selected.processing_error&&<p className="error">{selected.processing_error}</p>}{selected.status==="uploaded"||selected.status==="failed"||selected.status==="scan_failed"?<button className="upload" onClick={()=>{onScan(selected.id);close()}}>Scan bill</button>:<button className="upload" onClick={close}>Done reviewing</button>}<p className="muted" style={{marginTop:20}}>This card keeps you in the document queue. Click outside or × to close.</p></section></div>}</>}
