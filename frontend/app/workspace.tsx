'use client'
import Link from 'next/link'
import {ChangeEvent,useState} from 'react'
import {sample} from './sample'
import CustomerDashboard from './customer-dashboard'
import AssistantPanel from './assistant-panel'
import EvidenceSearch from './evidence-search'
import GstWorkspace from './gst-workspace'
const heads=['igst','cgst','sgst','cess']
const nav=[['overview','/','◫','Overview'],['gstworkspace','/gst/workspace','◈','GST Workspace'],['documents','/documents','▤','Documents'],['filing','/gst/filing','▦','GST Filing & ITC'],['search','/search','⌕','Search'],['assistant','/assistant','✧','Assistant']]
const titles:Record<string,string>={overview:'Your financial dashboard',documents:'Documents',filing:'GST Filing & ITC',search:'Find your evidence',assistant:'Your finance assistant',gstworkspace:'GST Workspace'}
export function money(value:unknown){if(value===undefined||value===null)return '—';const [w,f='']=String(value).split('.');return `${w.startsWith('-')?'-':''}₹${BigInt(w.replace('-','')||'0').toLocaleString('en-IN')}.${f.padEnd(2,'0').slice(0,2)}`}
function NavItem({item,view,period}:{item:string[];view:string;period:string}){return <Link className={`nav-item ${item[0]===view?'active':''}`} aria-current={item[0]===view?'page':undefined} href={`${item[1]}?period=${period}`}><span aria-hidden>{item[2]}</span>{item[3]}</Link>}
export default function Workspace({view,initial,children}:{view:string;initial:any;children?:React.ReactNode}){
 const [data,setData]=useState(initial),[message,setMessage]=useState(''),[busy,setBusy]=useState(false),[page,setPage]=useState(0),[arn,setArn]=useState(''),[filedAt,setFiledAt]=useState('')
 const period=data?.period||new Date().toISOString().slice(0,7),user=data?.user,profile=data?.profile,status=data?.filing?.state||'draft',locked=status==='locked'
 const sampleMode=Boolean(data?.demo_fallback&&data?.mode==='empty'),write=Boolean(user&&['client','tax_admin','admin'].includes(user.role)&&!data?.demo_fallback),manage=Boolean(user&&['tax_admin','admin'].includes(user.role)&&!data?.demo_fallback)
 const summary:any=sampleMode?sample.summary:data?.summary,docs=data?.documents||[],counts=data?.counts||{}
 async function request(path:string,body?:any){const c=await fetch('/api/auth/csrf');const token=c.ok?(await c.json()).token:'';const r=await fetch(path,{method:'POST',headers:{'x-csrf-token':token,...(body instanceof FormData?{}:{'content-type':'application/json'})},body:body instanceof FormData||typeof body==='string'?body:body?JSON.stringify(body):undefined});if(!r.ok){const e=await r.json().catch(()=>({}));throw Error(e.error?.message||(typeof e.detail==='string'?e.detail:null)||'The action could not be completed.')}return r}
 async function reload(){const r=await fetch(`/api/${view==='overview'?'dashboard':'workspace'}?period=${period}`,{cache:'no-store'});if(!r.ok)throw Error('Workspace refresh failed. Please retry.');setData(await r.json())}
 async function action(fn:()=>Promise<void>){setBusy(true);setMessage('');try{await fn()}catch(e){setMessage(e instanceof Error?e.message:'Please try again.')}finally{setBusy(false)}}
 async function upload(e:ChangeEvent<HTMLInputElement>){const f=e.target.files?.[0];if(!f)return;await action(async()=>{const body=new FormData();body.append('file',f);const r=await request(`/api/pipeline/upload?period=${period}`,body);const result=await r.json();setMessage(result.duplicate?'This bill is already in your documents.':'Bill received and queued for reading.');await reload()});e.target.value=''}
 async function importFile(e:ChangeEvent<HTMLInputElement>,kind:string){const f=e.target.files?.[0];if(!f)return;await action(async()=>{await request(`/api/gst/filing/${kind}?period=${period}`,await f.text());await reload();setMessage('Import complete. Your figures have been updated.')});e.target.value=''}
 async function transition(state:string){await action(async()=>{await request(`/api/gst/filing/transition?period=${period}`,{state,arn,filed_at:filedAt});await reload()})}
 async function generate(kind:string){await action(async()=>{const r=await request(`/api/gst/filing/generate/${kind}?period=${period}`);const url=URL.createObjectURL(await r.blob());const a=document.createElement('a');a.href=url;a.download=`${kind}_${period}_draft.json`;a.click();URL.revokeObjectURL(url);await reload()})}
 const uploadButton=write&&!locked?<label className="button primary"><input className="visually-hidden" type="file" accept="application/pdf,image/jpeg,image/png" disabled={busy} onChange={upload}/>＋ Upload bill</label>:null
 return <div className="bv-shell"><aside className="bv-sidebar"><Link className="bv-brand" href="/"><span className="brand-mark">bv.</span><span>Bills & Vouchers<small>Clarity in every return</small></span></Link><p className="nav-caption">WORKSPACE</p><nav aria-label="Main navigation">{nav.map(item=><NavItem key={item[0]} item={item} view={view} period={period}/>)}</nav><div className="sidebar-note"><span className="live-dot"/>Your evidence, connected.<small>Receive · Review · Prepare</small></div></aside><div className="bv-main"><header className="bv-header"><div className="client-identity"><strong>{profile?.legal_name||'Client profile incomplete'}</strong><div><span className={`chip ${profile?.gstin?'':'warning'}`}>{profile?.gstin||'GSTIN pending'}</span>{profile?.state_code&&<span className="chip">{profile.state_code==='33'?'Tamil Nadu':`State ${profile.state_code}`}</span>}</div></div><label className="period-control"><span>RETURN PERIOD</span><input aria-label="Return period" type="month" value={period} disabled={busy} onChange={e=>action(async()=>{if(view!=='demo')await request('/api/workspace/period',{period:e.target.value});window.location.href=`${nav.find(n=>n[0]===view)?.[1]||'/'}?period=${e.target.value}`})}/></label><details className="account-menu"><summary><span className="avatar">{user?.full_name?.[0]||'?'}</span><span>{user?.full_name||'Account'}<small>{user?.role||'Session'}</small></span><span aria-hidden>⌄</span></summary><div className="account-popover">{data?.clients?.length>1&&<label>Switch client<select value={data.client_id} onChange={e=>action(async()=>{await request('/api/workspace/client',{client_id:e.target.value});window.location.reload()})}>{data.clients.map((c:any)=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label>}<Link href="/settings">Account settings</Link><button onClick={()=>action(async()=>{await request('/api/auth/logout');window.location.href='/login'})}>Sign out</button></div></details></header>{sampleMode&&!data?.demo_lab&&<div className="sample-banner">Sample data — not your client&apos;s figures</div>}<main className="bv-content"><noscript><div className="notice">JavaScript is needed for uploads and filing actions. Your workspace is available to read.</div></noscript><div className="page-heading"><div><p className="eyebrow">FINANCE CONTROL PLANE</p><h1>{titles[view]||'Document review'}</h1><p className="muted">{view==='overview'?'Your validated financial position for the selected period.':view==='filing'?'Reconcile purchases, understand your credit and prepare the return.':'From the bills you receive to the return you prepare.'}</p></div>{view==='documents'&&uploadButton}</div>{message&&<div className="notice" role="status">{message}</div>}{!data?<section className="bv-card"><Empty text="Your workspace could not be loaded. Retry or use the account menu to sign in again."/><Link className="button secondary" href="/">Retry workspace</Link></section>:<>{!profile?.gstin&&!data?.demo_lab&&<div className="profile-notice"><span aria-hidden>ⓘ</span><div><strong>Your GST profile is being completed</strong><p>You can upload and review bills now. Add your GSTIN before validating or exporting a return.</p></div></div>}
 {view==='gstworkspace'&&<GstWorkspace data={data} money={money}/>}
 {view==='overview'&&<CustomerDashboard data={data} money={money}/>}
 {view==='documents'&&<><section className="metric-grid">{[['Documents',counts.documents,'This period','blue'],['Needs review',counts.review,'Awaiting your review','amber'],['Posted',counts.posted,'In the ITC ledger','green'],['Failed',counts.failed,'Could not be read','red']].map(([label,value,detail,tone])=><div className={`metric-card ${tone}`} key={label}><span>{label}</span><strong>{value||0}</strong><small>{detail}</small></div>)}</section><section className="bv-card"><div className="section-heading"><h2>Your document pipeline</h2><small className="muted">{data.pipeline?.updated_at?`Updated ${new Date(data.pipeline.updated_at).toLocaleDateString('en-IN')}`:'Awaiting your first bill'}</small></div><div className="pipeline-strip">{[['bronze','Received','Original bills kept safely'],['silver','Read & checked','Details extracted for review'],['gold','Ready for return','Validated entries in your ledger']].map(([key,title,desc],i)=><div className="pipeline-tile" key={key}><span className="step-number">0{i+1}</span><div><small>{key.toUpperCase()}</small><h3>{title}</h3><p>{desc}</p></div><strong>{data.pipeline?.[key]||0}</strong></div>)}</div></section><section className="bv-card"><div className="section-heading"><h2>Your source evidence</h2><button className="button quiet" disabled={busy} onClick={()=>action(reload)}>Refresh status</button></div><DocumentTable docs={docs} period={period}/></section></>}
 {view==='search'&&<EvidenceSearch period={period} money={money}/>}
 {view==='assistant'&&<AssistantPanel key={period+data.client_id} period={period}/>}
 {view==='filing'&&<>{sampleMode&&<p>Illustration: {sample.name}</p>}<section className="comparison-grid">{[['As booked','as_booked'],['Restricted to GSTR-2B','restricted_2b'],['Fully compliant','fully_compliant'],['If late suppliers file','if_late_file']].map(([label,key],i)=><div className={`comparison-card ${i===2?'selected':''}`} key={key}><span>{label}</span><strong>{money(summary?.[key])}</strong><small>Cash required</small></div>)}</section><div className="filing-grid"><section className="bv-card"><h2>Input credit & output tax</h2><p className="muted">Your net liability after tax-head set-off.</p>{summary?<><div className="table-scroll"><table><thead><tr><th>Tax head</th><th>Input</th><th>Eligible</th><th>Output</th><th>Net payable</th><th>Cash required</th></tr></thead><tbody>{heads.map(h=><tr key={h}><td className="uppercase">{h}</td>{['input_by_head','eligible_by_head','output_by_head','net_payable_by_head','cash_by_head'].map(k=><td className="money" key={k}>{money(summary[k]?.[h])}</td>)}</tr>)}</tbody></table></div>{profile?.is_eco_9_5&&<p>Section 9(5) ECO liability is cash-only and reported separately in 3.1.1.</p>}<p className="non-gst">Non-GST purchases <strong>{money(summary.non_gst)}</strong></p>{!data.outward_count&&<p className="notice">No sales invoices have been imported for this period.</p>}</>:<Empty text="Upload purchase invoices to start preparing this period." href={`/documents?period=${period}`} label="Open documents"/>}</section><section className="bv-card"><h2>Reconciliation</h2><p className="muted">Compare your books with GSTR-2B.</p>{summary?<div className="recon-list">{[['matched','Matched'],['unmatched_books','Awaiting GSTR-2B'],['unmatched_2b','Missing in books'],['amount_mismatch','Amount differences']].map(([key,label])=><div key={key}><span>{label}</span><strong>{data.matches.filter((r:any)=>r.match_status===key).length}</strong></div>)}</div>:<Empty text="Import GSTR-2B to compare supplier invoices."/>}{write&&!locked&&<div className="stack-actions"><label className="button secondary"><input className="visually-hidden" type="file" accept="application/json" disabled={busy} onChange={e=>importFile(e,'import-2b')}/>Import GSTR-2B</label><label className="button secondary"><input className="visually-hidden" type="file" accept="application/json" disabled={busy} onChange={e=>importFile(e,'outward')}/>Import sales invoices</label>{data.has_silver&&<button className="button secondary" disabled={busy} onClick={()=>action(async()=>{await request(`/api/gst/filing/recompute?period=${period}`);await reload()})}>Run ITC computation</button>}</div>}</section></div><section className="bv-card"><div className="section-heading"><div><h2>From booked to claimable</h2><p className="muted">Every decision has a rule reference.</p></div><span className="chip">{data.ledger.length} entries</span></div>{data.ledger.length?<><div className="table-scroll"><table><thead><tr><th>Invoice / line</th><th>Description</th><th>Decision</th><th>Rule</th><th>IGST</th><th>CGST</th><th>SGST</th></tr></thead><tbody>{data.ledger.slice(page*20,(page+1)*20).map((r:any)=><tr key={`${r.doc_id}-${r.line_no}`}><td>{r.invoice_no}<small>Line {r.line_no}</small></td><td>{r.description}</td><td><span className={`chip ${r.reason_code==='eligible'?'success':'warning'}`}>{r.reason_code}</span></td><td>{r.rule_ref}</td>{heads.slice(0,3).map(h=><td className="money" key={h}>{money(r[`${r.reason_code}_${h}`]||'0')}</td>)}</tr>)}</tbody></table></div><div className="pagination"><button disabled={page===0} onClick={()=>setPage(page-1)}>Previous</button><span>Page {page+1}</span><button disabled={(page+1)*20>=data.ledger.length} onClick={()=>setPage(page+1)}>Next</button></div></>:<Empty text="Validated invoices will appear here after ITC computation."/>}</section><section className="bv-card filing-panel"><div className="section-heading"><div><h2>Prepare & track your filing</h2><p className="muted">Validate → Generate JSON → Mark as filed → Lock period</p></div><span className="chip success">{status.replace('_',' ')}</span></div>{sampleMode?<p>Filing actions are disabled for sample figures.</p>:write?<><div className="filing-actions">{status==='draft'&&<button className="button primary" disabled={busy||!profile?.gstin||!summary} onClick={()=>transition('validated')}>Validate</button>}{['validated','json_generated'].includes(status)&&<><button className="button primary" disabled={busy} onClick={()=>generate('gstr3b')}>Generate GSTR-3B draft JSON</button><button className="button secondary" disabled={busy} onClick={()=>generate('gstr1')}>Generate GSTR-1 draft JSON</button></>}{manage&&status==='json_generated'&&<><input aria-label="ARN" placeholder="15-character ARN" value={arn} maxLength={15} onChange={e=>setArn(e.target.value.toUpperCase())}/><input aria-label="Filing date" type="date" value={filedAt} onChange={e=>setFiledAt(e.target.value)}/><button className="button primary" disabled={busy} onClick={()=>transition('filed')}>Mark as filed</button></>}{manage&&status==='filed'&&<button className="button primary" disabled={busy} onClick={()=>transition('locked')}>Lock period</button>}{locked&&<p>This period is locked.</p>}</div><small className="muted">JSON downloads are preparer drafts. Verify the portal schema before uploading.</small></>:<p className="muted">You have read-only access to this return.</p>}</section></>}{children}</>}<footer className="workspace-footer"><span>Bills & Vouchers</span><span>Keep the evidence. Know the numbers.</span></footer></main></div></div>
}
function Empty({text,href,label}:{text:string;href?:string;label?:string}){return <div className="empty-state"><span className="empty-icon" aria-hidden>▤</span><p>{text}</p>{href&&<Link className="button secondary" href={href}>{label}</Link>}</div>}
function DocumentTable({docs,period}:{docs:any[];period:string}){return docs.length?<div className="table-scroll"><table><thead><tr><th>Document</th><th>Supplier</th><th>Total</th><th>Pipeline stage</th><th>Status</th><th/></tr></thead><tbody>{docs.map(d=><tr key={d.doc_id}><td><strong>{d.original_filename}</strong><small>{new Date(d.uploaded_at).toLocaleDateString('en-IN')}</small></td><td>{d.silver?.supplier_name||'Waiting to be read'}</td><td className="money">{money(d.silver?.total)}</td><td><span className="stage-chip">{d.stage}</span></td><td><span className={`chip ${d.silver?.validation_status==='validated'?'success':d.silver?.validation_status==='failed'?'danger':''}`}>{d.silver?.validation_status?.replace('_',' ')||'Queued'}</span></td><td><Link href={`/documents/${d.doc_id}?period=${period}`}>Review →</Link></td></tr>)}</tbody></table></div>:<Empty text="No bills for this period yet. Upload a bill to begin."/>}
